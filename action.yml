name: 'Build OpenGD77'
description: ' '
inputs:
  branch:  # id of input
    description: 'Branch to checkout'
    required: true
    default: 'master'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.branch }}
